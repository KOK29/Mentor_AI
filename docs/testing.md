# Testing

Backend unit tests cover knowledge placement, password hashing, and JWT validation.

```bash
cd backend
pip install -r requirements.txt
pytest -q
```

Frontend verification:

```bash
cd frontend
npm install
npm run build
```
